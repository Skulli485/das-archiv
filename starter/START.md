# Schnellstart

```bash
# Terminal 1
cd server && bun start

# Terminal 2
cd client && bun install && bun run dev
```

Client auf http://localhost:5173. Zeigt „Grenze erreichbar", wenn beide laufen.
Weiter mit `../aufgaben/01-zwei-prozess-geruest.md`.
